<% include ../partials/header %>
<div class="row">
    <h2><%= user.username %> these are your products currently for sale</h2>
     <div class="row"><br>
  <div class="grid-container">
      <% res1.forEach(function(product) { %>
         <box><div class="item">
                 <div class="item">
                   <h1 class="itemp">Username: <%= product.userid %></h1>
                   <h1 class="itemp"><%= product.title %></h1>
                   <h1 class="itemp">Brand: <%= product.brand %></h1>
                   <image src ="/<%= product.image %>" class="pimage"></image>
                    <h1 class="itemp">Price: €<%= product.price %></h1>
                   <a href="/deletesql/<%= product.id%>" class="button">Remove item</a>
                   <a href="/editsql/<%= product.id%>" class="button">Edit item</a>
            </label>
            
         </div>
         </form>   
    </div></box>
  <% }); %>
</div>
<% include ../partials/footer %>
</body>
</html>
